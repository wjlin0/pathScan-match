rules:
  - name: "Microsoft-IIS"
    matchers:
      - type: regex
        part: header
        name: iis
        group: 1
        regex:
          - '(?i)Server: .*?(Microsoft-IIS[/]?[\d\.]*)'
          - 'Server: .*?(Microsoft-HTTPAPI/\d+\.\d+).*?'